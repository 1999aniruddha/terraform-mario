apiVersion: v1
kind: Service
metadata:
  name: mario-service
  labels:
    app: mario
spec:
  type: ClusterIP
  selector:
    app: mario
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
